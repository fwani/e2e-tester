# 확정 디자인이 정의하지 않은 상태 기록

**요구사항**: DC-009 · 완료 판정 SC-110 (기록 없는 임의 결정 0건)

확정 디자인은 화면당 한 상태만 보여준다. 실제 동작에 필요한 나머지 상태(로딩·빈 목록·
오류·긴 목록·텍스트 넘침)는 구현자가 정해야 한다. **그 결정을 전부 여기 남긴다.**

`근거` 는 필수다. "가장 가까운 확정 디자인 표현을 따른다"(DC-009)를 지켰다는 증거가
근거 없이는 성립하지 않는다. **근거를 댈 수 없는 결정은 해석이고 DC-001 위반이다.**

| 화면 | 상태 | 근거 (어느 확정 디자인의 어느 표현) | 결정 |
|---|---|---|---|
| ProjectSetup | 로딩 | 확정 디자인에 대응 화면이 없다(DC-010). TestList.dc.html 의 흐린 텍스트 색 #9A968A 를 따랐다 | 「프로젝트를 찾는 중…」 을 흐린 색으로 표시 |
| ProjectSetup | 빈 목록 | 확정 디자인에 대응 화면이 없다(DC-010). 8화면의 카드 표현(#FFFDF6 + 3px 잉크 테두리)을 따랐다 | 무엇을 하면 되는지 두 갈래로 안내하는 카드 |
| ProjectSetup | 오류·경고 | 확정 디자인에 오류 배너가 없다. RunResult.dc.html 의 FAIL 색(#D9502F/#FBEEEA)과 경고 노랑(#F5D000/#FFF9D6)을 따랐다 | 화면 상단 배너. 계약 메시지를 그대로 표시 |
| ProjectSetup | 긴 경로 넘침 | 확정 디자인에 경로 표시가 없다. 모노 글꼴과 흐린 색만 따르고 넘침은 말줄임 | ellipsis + title 속성으로 전체 경로 제공 |
| TestList | 로딩 | 확정 디자인은 행이 있는 상태만 보여준다. 목록 테두리 안에 흐린 색(#9A968A) 텍스트로 넣었다 | 「불러오는 중…」 |
| TestList | 빈 목록 | 같음. 검색 결과 없음과 테스트 없음을 구분해야 사용자가 다음 행동을 안다 | 무엇을 하면 되는지 안내하는 한 줄 |
| TestList | 실행한 적 없는 테스트의 상태 칩 | 확정 디자인에 PASS·FAIL 두 상태만 있다. 같은 칩 형태(28px·2px 테두리)에 배경만 #EFEBE0 으로 | 「—」 칩 |
| TestList | `⋯` 메뉴의 내용 | 확정 디자인은 `⋯` 버튼만 그리고 열린 모습은 정의하지 않는다. 8화면의 카드 표현(3px 테두리 + 5px 하드 그림자)을 따랐다 | 정의 보기·이름·삭제 세 항목 |
| TestList | 삭제 확인 | 확정 디자인에 없다. 되돌릴 수 없는 조작이라 확인이 요구사항이다(FR-007). 실패 요약과 같은 자리에 #A83A22 로 | 행 안에서 확인. 어느 테스트인지 보면서 결정하게 한다 |
| TestList | 정의 파일 오류 배너 | 확정 디자인에 없다. 경고 노랑(#F5D000/#FFF9D6)을 따랐다 | 읽지 못한 파일 목록 |
| CreateTest | URL 형식 오류 | 확정 디자인에 오류 표현이 없다. RunResult.dc.html 의 실패 색(#A83A22)을 따랐다 | 시작 URL 아래 한 줄 |
| CreateTest | AI 사용 불가 안내 | 확정 디자인에 없다. 경고 노랑(#F5D000/#FFF9D6)과 2px 테두리를 따랐다 | AI 카드 안에 넣고 버튼을 비활성화 (DR-021) |
| AiRecord | 지시문 작성 중 | 확정 디자인은 「AI 수행 중」 하나만 보여준다. 같은 화면을 composing 으로 그린다 — 새 화면을 만들지 않았다(DC-007) | 지시문 칸을 textarea 로, 상태 알약을 「지시문 작성」으로 |
| AiRecord | AI 수행 실패 | 확정 디자인에 없다. RunResult.dc.html 의 실패 카드(#FBEEEA/#A83A22)를 따랐다 | 지시문 아래 전용 영역. 세션 상태와 무관하게 그린다 (DR-020) |
| AiRecord | Step 0개 | 확정 디자인은 Step 이 쌓인 상태만 보여준다 | 흐린 색 한 줄 |
| AiRecord | 테스트 이름 입력 | 확정 디자인에 이름 칸이 없는데 저장에는 이름이 필요하다(FR-028) | 「테스트로 저장」 위에 3px 테두리 입력칸 |
| Main(Runner) | Step 0개 | 확정 디자인은 Step 이 쌓인 상태만 보여준다 | 흐린 색 한 줄 |
| Main(Runner) | 탭 목록 | 확정 디자인에 탭 스트립이 없다(단일 탭만 보여준다). 미러 위에 얹었다 | TabStrip 을 브라우저 껍데기 위에 |
| RunnerPaused | 검토(review) 상태 | 확정 디자인에 없다. 002 가 새로 만든 상태다(DR-010). PAUSED 화면을 재사용하되 브라우저 명령을 감췄다 | 상태 알약 REVIEW, 「계속하기」 제거, 「나가기」 |
| RunnerPaused | Assertion 추가 폼 | 확정 디자인은 버튼만 그리고 폼은 정의하지 않는다 | 도구 격자 아래에 3px 테두리 폼 |
| RunnerPaused | 순서 변경 패널 | 같음. 드래그 대신 위·아래 버튼 — 목록이 200개까지 간다(research R8) | Step 목록 아래 패널 |
| RunnerPaused | 저장 영역 | 확정 디자인에 저장 칸이 없는데 저장에는 이름이 필요하다(FR-028) | 도구 아래 2px 구분선 + 이름칸 + 저장 |
| Takeover | AI 멈춤(녹화 전) | 확정 디자인은 「사람이 녹화 중」만 보여준다. 같은 붉은 알약에 문구만 바꿨다 | 상태 알약 「AI 멈춤」, 붉은 띠 감춤 |
| RunResult | PASS 상태 | 확정 디자인은 FAIL 만 보여준다. 같은 구조에 색만 #2E9455 로 | 상태 알약·칩·Step 표식만 초록 |
| RunResult | 건너뛴 Step | 확정 디자인에 없다. RunnerPaused.dc.html 의 미실행 행 표현(opacity 0.55 + 빈 테두리)을 따랐다 | 흐리게 + 소요시간 — |
| RunResult | TRACE 탭 | 확정 디자인에 탭이 있으나 서버가 501 을 준다(001 의 알려진 차이). 있는 것을 빼지 않으려고 그리되 비활성 | disabled 버튼 + 안내 문구 |
| StepInspector | 겹침 표시 | 확정 디자인은 640×1140 독립 artboard 다. 세션 화면 위에 오른쪽에서 겹쳐 띄운다 | 반투명 잉크 배경 + 오른쪽 정렬 |
| 전 화면 | 창이 기준 폭보다 좁을 때 | DC-011 — 기준 폭을 유지하고 스크롤한다. 임의 재배치를 하지 않는다 | Artboard 가 overflow-x: auto |
