# 기준선 — 009 착수 시점 (T001·T002)

**측정 시각**: 2026-09-08 | **커밋**: `16b1224` | **브랜치**: `009-step-editing-flow`

이 문서는 SC-501~SC-510 이 "몇에서 몇으로 바뀌었는가"를 주장할 수 있게 하는 **이전 값**이다.
T057 이 이후 값을 여기 나란히 적는다.

## 측정 방법

| 지표 종류 | 어떻게 쟀나 |
|---|---|
| 개수·호출처 | `grep` 전수 + 기존 검사의 단언값 |
| 조작 횟수 | **코드 경로 추적** — 각 조작이 어느 핸들러를 지나는지 읽어 셌다. 살아 있는 화면에서의 확인은 quickstart 실측(T032·T041·T052)이 한다 |
| 검사 건수 | 실제 실행 |

**조작 횟수를 코드로 센 이유를 밝힌다.** 브라우저를 띄워 세는 것이 더 강한 증거지만, 지금
필요한 것은 「무엇이 몇 번이었나」의 고정이고 그 값은 핸들러 경로에서 결정된다. 화면에서의
확인은 각 이야기 종료 시점에 quickstart 절차로 따로 한다 — 그때는 바뀐 값과 함께 본다.

---

## 1. 개수와 연결 상태

| 지표 | 값 | 근거 |
|---|---|---|
| 조작 수 | **34** | `frontend/src/lib/actions.ts` 의 `ACTION_IDS` (6개 묶음 합) · `CapabilityCoverage.test.ts` 가 34로 단언 |
| 국면 수 | **8** | `frontend/src/lib/phase.ts` 의 `PHASES` |
| 표 칸 수 | **272** (8 × 34) | 위 둘의 곱 |
| `insertStep` 호출처 | **0** | `grep -rn insertStep frontend/src` = `api/client.ts:516` 정의 1건뿐 |
| `rowActions` 를 넘기는 화면 | **0** | `grep -rln rowActions frontend/src/pages frontend/tests` = 0 |
| 요소 지목 없이 Step 을 넣는 방법 | **없음** | `tests.py` 의 `EditOp` 에 `insert` 가 없다 |
| Step 삭제 확인 절차 | **없음** | 확인 상태(`confirming`)를 가진 화면은 `pages/TestList.tsx`(테스트 삭제)와 `SessionScreen`(나가기·닫기)뿐 |
| 경고 문장이 Step 번호를 박는가 | **박는다** | `execution/step_edits.py:100` `already_executed_warning` → `f"step {index + 1:02d} 은 …"` |

---

## 2. 조작 횟수

### 2-1. Step 하나를 세 칸 아래로 옮긴다

| 화면 | 횟수 | 경로 |
|---|---|---|
| 편집 (`EditView`) | **6** | 아래 Step 3개에 대해 각각 〈행 이름 클릭으로 선택 1〉 + 〈팔레트 「위로 옮기기」 1〉. `runAction("step.reorder")` 가 `move(currentIndex, -1)` 뿐이므로 아래로 옮기는 길이 없다 |
| 일시정지 (`SessionScreen`) | **5** | 〈팔레트 「순서 변경」 1〉 + 〈패널에서 ↓ 3〉 + 〈「적용」 1〉. 목록의 사본인 `ReorderPanel` 을 거친다 |

**옮기려는 Step 이 아닌 다른 Step 을 고르는 조작이 편집 화면에서 3회**다. 이것이 SC-503 이
없애려는 것이다.

### 2-2. Step 하나를 지운다

| 화면 | 횟수 | 경로 |
|---|---|---|
| 편집 | **2** | 〈행 이름 클릭으로 선택 1〉 + 〈팔레트 「Step 삭제」 1〉 |
| 일시정지 | **2** | 같음 |

> **명세 SC-504 의 「현재 3회」는 틀렸다. 실측 2회다.** 팔레트가 항상 그려져 있어(토글이
> 없다) 여는 조작이 들어가지 않는다 (`ActionPalette` 는 `stepFooter` 로 상시 렌더).
>
> 이 지표는 **횟수로 개선을 주장할 수 없다.** 목표 상태(행에서 1회 + 확인 1회)도 2회다.
> 실제로 바뀌는 것은 둘이다 — ① 목록에서 화면 아래 팔레트로 내려가는 **왕복이 없어진다**
> ② **확인 없이 지워지는 경로가 없어진다**. SC-504 를 그 두 가지로 고쳤다.

### 2-3. 요소 지목이 필요 없는 Step 을 원하는 자리에 넣는다

| 화면 | 횟수 | 경로 |
|---|---|---|
| 편집 | **불가능** | 삽입 연산이 없다 |
| 일시정지 | **불가능** (직접 입력) | `insertStep` API 는 있으나 부르는 화면이 없다 |

**우회로**: 브라우저를 열어 그 자리까지 재생한 뒤 녹화·자연어·검증 폼을 쓴다. 사용자가
스스로 조립해야 하는 단계가 **5개**다 (관찰 M-04).

### 2-4. 순서 변경 시 화면에 뜨는 Step 목록 수

| 화면 | 값 |
|---|---|
| 편집 | **1** |
| 일시정지 | **2** — `StepList` + `ReorderPanel` (목록의 사본) |

---

## 3. 회귀 기준선 (T002)

| 대상 | 명령 | 결과 |
|---|---|---|
| 백엔드 (브라우저·시간 제외) | `uv run pytest -m "not browser and not timing" -q` | **1197 passed** (23.8s) |
| 프론트엔드 | `npm test -- --run` | **53 파일 · 682 tests passed** (9.1s) |

**이후 어느 단계에서든 이 수가 줄면 회귀다.** 늘어나는 것은 정상이다 — 이 기능은 검사를
더한다.

브라우저 의존 백엔드 검사(`-m browser`)는 이 기준선에서 제외했다. 실행에 Playwright
chromium 이 필요하고 시간이 길어, 이 기능이 그 경로를 건드리는 단계(T023·T035)에서 따로
돌린다.

---

## 3-1. US1 이후 실측 (T032)

**측정 방법**: `frontend/tests/StepInsert.test.tsx` 가 실제 `EditView` 를 렌더해 조작
경로를 그대로 지난다. 아래 횟수는 그 검사가 실제로 누르는 버튼 수다 — 값 입력(타이핑)은
조작으로 세지 않는다. 살아 있는 브라우저에서의 확인은 T052(quickstart 전량)가 한다.

| 지표 | 이전 | 이후 | 판정 |
|---|---|---|---|
| 요소 지목 없이 Step 을 넣는 조작 수 (편집 화면) | **불가능** | **2회** — 「직접 입력으로 Step 추가」 → 「넣기」 | **SC-501 충족** (3회 이내) |
| 그때 만들어지는 세션 | — | **0개** — 정의 조회 하나로 끝난다 | 브라우저를 열지 않는다 |
| 저장까지 포함 | — | 3회 (+「저장」) | 저장은 별도 조작이다 |
| 일시정지 중 직접 입력 삽입 | **불가능** | **2회** — 같은 두 조작 | FR-290 |

**요소를 요구하는 종류**(클릭·입력·선택·마우스 올리기·끌어놓기)는 같은 자리에 비활성으로
남고 갈 길(브라우저 열어 이 자리에서 멈추기)이 함께 보인다 — 감춰진 조작 **0개**.

---

## 3-2. US2 이후 실측 (T041)

**측정 방법**: `frontend/tests/InsertViaBrowser.test.tsx` 가 실제 `EditView` 와
`SessionWorkbench` 를 렌더해 흐름을 지난다. 백엔드 쪽 목표 정지·도달 전 실패는
`backend/tests/integration/test_pause_before_target.py` 가 **실제 브라우저**로 확인한다.

| 지표 | 이전 | 이후 | 판정 |
|---|---|---|---|
| 「그 앞까지 재생하고 멈춘 상태 + 추가 도구 열림」 도달 조작 수 | 사용자가 조립할 단계 **5개** | **1회** — 「브라우저 열어 이 Step 앞에서 멈추기」 | **SC-502 충족** |
| 그 조작이 대신 해 주는 일 | — | 저장 → 세션 생성(목표 지정) → 목표 앞까지 재생 → 일시정지 → 기록 시작 | FR-291 |
| 재생 중 목표 표시 | 없음 | 「Step nn 앞에서 멈춥니다 · 지금 Step mm」 | FR-293 |
| 새로 고침 후 목표 유지 | — | **유지** — `SessionView.pause_before_index` | research R5 |
| 도달 전 실패 구별 | 「일시정지됨」만 | 「Step nn 에 도달하기 전에 Step mm 에서 실패했습니다」 | FR-294 |
| 결과 화면 → 그 Step 을 고른 편집 화면 | **이미 동작** | 동작 (검사로 고정) | FR-297 |

**FR-297 은 코드 변경이 없었다.** `ResultView` 의 `nav.editStep` 이 이미 `stepId` 를
넘기고 `App` 이 `focusStepId` 로 전달한다. 없던 것은 그 사실을 고정하는 검사였고, 그것을
더했다.

---

## 3-3. US3 이후 실측 (T052)

**측정 방법**: `frontend/tests/StepRowActions.test.tsx` 가 실제 `EditView` 와
`SessionWorkbench` 를 렌더해 **행에서** 누른다. 아래 횟수는 그 검사가 실제로 누르는
버튼 수다.

| 지표 | 이전 | 이후 | 판정 |
|---|---|---|---|
| 세 칸 아래로 (편집) | **6회** (그중 3회가 다른 Step 고르기) | **3회** — 그 행의 「아래로」 3번 | **SC-503 충족** |
| 세 칸 아래로 (일시정지) | **5회** (패널 열기 + ↓×3 + 적용) | **3회** — 같은 조작 | **SC-503 충족** |
| 다른 Step 을 고르는 조작 | 3회 | **0회** | FR-298 |
| 순서 변경 시 화면의 Step 목록 수 (일시정지) | **2개** | **1개** — `ReorderPanel` 제거 | **SC-505 충족** |
| Step 삭제 시 목록↔팔레트 왕복 | **1건** | **0건** — 행 안에서 끝난다 | **SC-504 충족** |
| 확인 없이 지워지는 경로 | **있음** | **없음** — 행 안에서 확인 | **SC-504 충족** |
| 아래로 옮기기 | **없음** | 있음 | FR-299 |
| 끝단 방향 | (없던 조작) | 자리를 남기고 비활성 + 이유 | FR-300 |
| 행 조작의 접근 가능한 이름 | — | 「〈이름〉 위로 옮기기」·「〈이름〉 아래로 옮기기」 | **SC-507** |

### 구현 중 정정한 것 — 조작의 「자리」

계약 초안은 팔레트가 이동·삭제 자리를 행에 **양도**한다고 적었다. 그것은 이 저장소의 두
검사와 동시에 성립할 수 없다 — 행에 `data-action` 을 달면 「한 조작에 한 자리」가 깨지고,
팔레트에서 숨기면 「감춰진 조작 0건」이 깨진다.

`step.select` 의 선례가 답이었다: **자리는 담는 것이 선언하고 행은 사례다.** 팔레트가
`data-action` 을 갖고 행은 `data-row-action` 을 갖는다. 팔레트 경로도 그대로 동작한다 —
표가 거짓말하지 않아야 하기 때문이다. 계약 §3-3-0 에 그 사실을 적었다.

---

## 4. 이후 값 (T057 이 채운다)

| 성공 기준 | 이전 | 이후 | 판정 |
|---|---|---|---|
| SC-501 요소 지목 없는 삽입 조작 수 | 불가능 | **2회** | **충족** (T032) |
| SC-502 「이 앞에 추가」 도달 단계 수 | 5 | **1회** | **충족** (T041) |
| SC-503 세 칸 아래로 (편집) | 6 | **3회** | **충족** (T052) |
| SC-503 세 칸 아래로 (일시정지) | 5 | **3회** | **충족** (T052) |
| SC-504 삭제 왕복 / 확인 없는 경로 | 왕복 1회 · 확인 0 | **왕복 0 · 확인 있음** | **충족** (T052) |
| SC-505 순서 변경 시 목록 수 (일시정지) | 2 | **1개** | **충족** (T052) |
| SC-506 표 미정 칸 | 0 (272칸) | **0 (288칸)** | **충족** (T007) |
| SC-507 키보드 경로 | 팔레트만 | **행 조작 24개 전부 이름 있는 button** | **충족** (T052) |
| SC-508 실행 중 활성 오표시 | 0 | **0** — 세 입구 전부 잠김 · 화면 비활성 | **충족** (T051·T054) |
| SC-509 기존 추가 경로 3종 | 동작 | **동작 · 자리도 그대로** | **충족** (T053) |
| SC-510 왕복 무결성 | 삽입 경로 없음 | **넣고→저장→재확인→실행 통과** | **충족** (T023) |

**SC-501~SC-510 열 개 전부 충족.** 달성하지 못한 항목은 없다.

### 검사 수의 변화 (T056 전량 실행)

| 대상 | 착수 시점 | 완료 시점 | 늘어난 것 |
|---|---|---|---|
| 백엔드 (브라우저·시간 제외) | 1197 | **1282** | +85 |
| 백엔드 브라우저 (009 경로) | — | **7** | 왕복 무결성 2 · 목표 정지 5 |
| 프론트엔드 | 53파일 682 | **56파일 725** | +3파일 +43 |
| L1 대조 칸 | 625 (형태 25) | **725 (형태 29)** | +100 — 새 정본 형태 4종 |

**줄어든 것은 없다** — 회귀 0건. 그 밖에 통과한 기계 검증: 스키마 드리프트 없음 ·
임포트 경계 3계약 KEPT · ruff 통과 · 정본 18장 동일 · 정본 소비 위반 0(색 0 · 인라인 0 ·
팔레트 밖 0종).

### converge 1회차가 찾은 것 (Phase 7)

전량 검사가 초록인 뒤에도 갭이 넷 남아 있었다. 검사가 잡지 못하는 종류였다.

| # | 무엇 | 왜 검사가 놓쳤나 | 고침 |
|---|---|---|---|
| T059 | 일시정지 국면 팔레트의 「위로/아래로 옮기기」가 **활성인데 동작하지 않았다** (고른 Step 이 없을 때) | `CapabilityUI` 는 「비활성인데 이유가 없는 조작」과 「자리가 둘인 조작」을 세지, 「활성인데 아무 일도 안 하는 조작」은 셀 수 없다 | `STEP_SCOPED` 에 두 조작 추가 + 검사 2건 |
| T060 | 새 정본 형태 넷(`.srow-ops`·`.op` 계열)이 **L1 값 대조를 받지 않았다** | `design_render.py` 의 `FORMS` 가 손으로 유지되는 목록이다 — 008 이 고친 V-09 와 같은 종류의 구멍 | `FORMS` 25 → 29 (725칸). 자식 선택자용 `NESTED` 를 더해 **실제 값이 재어지는지** 실측 확인 |
| T061 | 확정 디자인에 「이 앞에 추가」 입력면과 「미저장」 칩이 없었다 | 대조표의 L3 는 **사람이** 채운다. 기계가 없는 요소를 세지 않는다 | `Main.dc.html` 에 둘 다 그렸다 (새 정본 클래스 없이 기존 형태로) |
| T062 | 화면의 종류 목록이 **손으로 쓴 타입**이었다 | 타입이 서로 파생되지 않으면 컴파일러가 볼 것이 없다 | 생성 타입에서 파생 + `Assert<>` 단언. **양방향 실측** — 요청 타입에서 하나를 빼면 4곳, 생성 타입에 하나를 더하면 단언이 직접 터진다 |

T060·T062 는 「가짜 초록」을 없앤 것이다 — 검사가 있는데 아무것도 검증하지 않는 상태였다.

### converge 2회차 (Phase 8)

| # | 무엇 | 고침 |
|---|---|---|
| T063 | 편집 국면의 `browser.openAt` 이 「정의가 편집 가능한가」로 좁혀지지 않아, 다른 세션이 잡고 있어도 활성이었다 (누르면 409) | 셀을 `ON` → `cond("C7")`. 해소 방법은 이미 `session.open` 이었다 |

T059 와 **같은 종류**다 — 활성인데 서버가 거절하는 조작. 009 가 이 조작을 다섯 걸음에서
한 걸음으로 만들었으므로(FR-291) 눌리는 빈도가 크게 늘어 그 경로를 남겨 둘 수 없었다.

**3회차 대조: 갭 0건.**

### 하나 정정했다

명세 SC-504 의 「현재 3회」가 실측과 달랐다(2회). 팔레트에 토글이 없어 여는 조작이
들어가지 않는다. 목표 상태도 2회이므로 **횟수로는 개선을 주장할 수 없어** 기준을 왕복
0건·확인 있음으로 다시 세웠다 (§2-2). 명세·quickstart·tasks 를 함께 고쳤다.
